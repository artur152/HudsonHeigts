<?php

function hudson_form_alter( &$form, &$form_state, $form_id ) {
    if ($form_id === 'search_block_form') {
        $form['keys']['#attributes']['placeholder'] = t( 'search' );
    }
    if ($form_id === 'search_block_form') {
        $form['keys']['#attributes']['placeholder'] = t( "wha ya'll looking for?" );
    }
}
